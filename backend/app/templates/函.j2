{% if subtype == "陳情回函" -%}
{# === 陳情回函：書信體 === #}
{{ receiver_name | default("○○○") }}君：您好

　　您於{{ reference_date | default("○年○月○日") }}{{ reference_channel | default("來信") }}，{{ reference_subject | default("詢問相關事宜") }}，為您說明如下：
{% for item in explanation_items -%}
　{{ cn_number(loop.index0) }}{{ item | trim_period }}。
{% endfor %}

　敬祝
健康快樂
{{ sender }}　敬復

{% else -%}
{# === 標準函格式 === #}
{{ sender }}　{{ doc_type_label | default("函") }}

受文者：{{ receiver }}
發文日期：{{ doc_date | default("中華民國　　年　　月　　日") }}
{% if doc_number -%}
發文字號：{{ doc_number }}
{% endif -%}
速別：{{ speed | default("普通件") }}
{% if attachment_line -%}
附件：{{ attachment_line }}
{% endif %}

{% if subtype == "復函" -%}
主旨：復{{ subject_detail | trim_period }}，{{ expectation }}。
{% elif subtype == "轉函" -%}
主旨：函轉{{ subject_detail | trim_period }}，{{ expectation }}。
{% else -%}
主旨：{{ opening }}{{ subject_detail | trim_period }}，{{ expectation }}。
{% endif -%}
{% if explanation_items -%}
說明：
{% for item in explanation_items -%}
　{{ cn_number(loop.index0) }}{{ item | trim_period }}。
{% endfor -%}
{% endif -%}
{% if action_items -%}
辦法：
{% for item in action_items -%}
　{{ cn_number(loop.index0) }}{{ item | trim_period }}。
{% endfor -%}
{% endif %}

正本：{{ recipients_main | trim_period }}
{% if recipients_cc -%}
副本：{{ recipients_cc | trim_period }}
{% endif -%}
{% endif -%}
